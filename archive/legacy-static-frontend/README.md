# Legacy static frontend archive

This folder preserves the pre-Angular single-file dashboard UI that used to ship from `public/index.html`.

It is intentionally archived for reference only.

The active shipped frontend is now the Angular build served from `frontend/dist/frontend/browser`.
